version: "3.9"
services:
  bot:
    # Use file-backed secret in production
    environment:
      - DISCORD_BOT_TOKEN_FILE=/run/secrets/discord_bot_token
    # Wire the secret (for Swarm/compatible platforms). For vanilla Compose, mount
    # a file to /run/secrets/discord_bot_token instead.
    # secrets:
    #   - discord_bot_token

# secrets:
#   discord_bot_token:
#     external: true
